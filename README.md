# Tally-Bridge-Connector
A lightweight Go micro-server with a Fyne GUI that connects to Tally Prime’s local XML API, processes data requests from a cloud server, converts XML to JSON, and securely exposes its own endpoints over the internet using Cloudflare Tunnel.
